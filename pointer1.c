//check the size of pointer variable
#include<stdio.h>
int main()
{
  int*ptr;
  float*fptr;
  char*cptr;
  double*dptr;
  printf("size of integer pointer=%d bytes\n",sizeof(ptr));
  printf("size of float pointer=%d bytes\n",sizeof(ptr));
  printf("size of char pointer=%d bytes\n",sizeof(ptr));
  printf("size of double pointer=%d bytes\n",sizeof(ptr));
}
