#include<stdio.h>
#include<string.h>
int main()
{
    char name[10];
    printf("Enter Name\n");
    //scanf("%s",name);
    gets(name);
    //printf("Name is %s",name);
    puts(name);
    puts;
    return 0;
}
