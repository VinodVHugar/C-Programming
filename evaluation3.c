#include<stdio.h>
int sum(int primenum1 ,int primenum2 );
int main()
{
    int primenum1,primenum2,sum ;
    printf("enter the value of N\n");
    scanf("%d",&primenum1,&primenum2);
    sum=primenum1+primenum2;
    printf("sum=%d\n");
    return 0;

}
int sum(int primenum1,int primenum2)

{
   for(i=3,i<=N,i++);
   {
       int s,
       s=primenum1+primenum2;
       return sum;

   }

